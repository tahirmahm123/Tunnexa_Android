package com.tunnexa.android.di

/**
 * RepositoryModule
 * 
 * Note: ServerRepository is already annotated with @Singleton and @Inject,
 * so Hilt can automatically provide it without explicit @Provides methods.
 * This module is kept for future repository dependencies if needed.
 */

